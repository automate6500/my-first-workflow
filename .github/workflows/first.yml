name: first

on: push

    branches:
        - master
        - develop

jobs:
    job1:
        name: First Job
        runs-on: ubuntu-latest
        steps:
            - name: Step one
              uses: actions/checkout@v1
            - name: Step two
              run: ls
    job2:
        name: Second Job
        runs-on: windows-latest
        steps:
            - name: Step one
              uses: actions/checkout@v1
            - name: Step two
              run: dir

